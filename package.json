{
  "name": "skillhub-formation-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix client && npm install --prefix server && npx prisma generate --schema=server/prisma/schema.prisma",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "test": "npm test --prefix server"
  }
}
